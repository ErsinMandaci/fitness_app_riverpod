import 'package:flutter/material.dart';

class ColorConstans {
  const ColorConstans._();

  static const Color midnightBlue = Color.fromARGB(255, 3, 14, 20);
  // static const Color textColor = Color.fromARGB(255, 255, 255, 255);
  static const Color textColor2 = Color.fromARGB(255, 255, 255, 0);
  static const Color cardTextColor = Colors.white;

  static const Color cardColor = Color(0xFF242632);
}
